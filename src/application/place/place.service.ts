export class PlaceService {
    // getAll()
    // getById(placeId)
    // create(address, city, postalCode, country)
    // delete(placeId)
    // addField(placeId, field)
    // removeField(placeId, fieldId)
}