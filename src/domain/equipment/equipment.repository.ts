export class EquipmentRepository {
    // getAll()
    // getById(materialId)
    // getByName(name)
    // create(name, quantity)
    // delete(materialId)
    // updateQuantity(materialId, quantity)
}